# Skins -- the single source every themed surface is generated from.
#
# Before this file existed the palette was maintained as four hand-synced
# copies, and the header comment in each one said so: "Change a value here and
# change it there too -- there is no single source the four formats can
# share." That was tolerable for one palette and not for several, so `skinctl`
# renders every format from the tokens below.
#
# The token names come from the Retro RPG launcher design handoff. Every
# surface in that design reads the same sixteen colours, so the same sixteen
# are all this file defines. Do not add a token here without also teaching
# skinctl what to do with it.
#
#   bg       desktop / the void behind the frame
#   shadow   outer 5px halo and hard drop shadow; darker than everything else
#   window   window fill behind the grid
#   outer    frame rule, title tab fill, big numerals
#   inner    cell and panel rules, dividers, EMPTY METER BLOCKS
#   cell     cell and inset fill
#   strip    detail strip / secondary panel fill
#   text     primary text
#   body     body copy
#   dim      tags, key hints, inactive
#   accent   cursor glyph, active selector, filled meters
#   outline  selection outline around the highlighted cell
#   cmd      category hue -- Terminal
#   net      category hue -- Browser
#   txt      category hue -- Editor
#   snd      category hue -- Music
#
# The one hard constraint, from the handoff: `inner` is the empty-meter track,
# so it must stay clearly darker than every colour that fills a meter (accent,
# outer, text and the four category hues) or a half-full bar stops reading as
# half full. `skinctl lint` checks this.
#
# Order matters. The launcher binds number keys to skins by position, so the
# nth entry below is key n, and the first entry is the default when no skin
# has been chosen. DREAM LAND is first: it is the machine's daily palette.
# The 2026-08 prune cut the roster from eleven skins to these four; the old
# ones (ember included) live in git history and on the archive/dream-land-mascot
# tag if they are ever wanted back.

# Battery and peripheral health only. These are deliberately skin-independent:
# a warning that changes colour with the theme is not a warning. The POWER OFF
# button in the control center also uses `critical`.
[thresholds]
ok       = "#3ddc84"
warn     = "#ffc53d"
critical = "#ff4a1f"

# Status-condition chip hues, also skin-independent (creature-shell handoff):
# SLP is suspend, BRN is thermal throttling, PAR is latency/packet loss.
slp      = "#7cb8ff"
brn      = "#ff7a3c"
par      = "#ffd36b"

# Behaviour defaults. A skin overrides these in its own [skins.behavior]
# table.
[defaults]
glyph      = "▶"
radius     = "0px"
blink      = "1s"
shine      = "none"
menu_word  = "LAUNCH"
empty_word = "EMPTY"


# ---------------------------------------------------------------------------
# DREAM LAND -- the default. Kirby-flavoured: a night-sky purple field with
# pink chrome and a warm star accent. Rounded, faster blink, star cursor,
# a highlight along the top of filled meter blocks, and its own faces --
# Lilita One for labels, Baloo 2 for body copy (both in the fonts package).
# The bar sits on the bottom edge, where the HUD lives in the games.
[[skins]]
slug    = "dream-land"
name    = "DREAM LAND"
bg      = "#0d0a26"
shadow  = "#05030f"
window  = "#241a5c"
outer   = "#ff5fa2"
inner   = "#2a2170"
cell    = "#17123f"
strip   = "#17123f"
text    = "#f4eaff"
body    = "#cbb6f0"
dim     = "#8a76c4"
accent  = "#ffcf7a"
outline = "#ff5fa2"
cmd     = "#7de3c8"
net     = "#6aa8ff"
txt     = "#ffb26b"
snd     = "#ff5fa2"

[skins.behavior]
glyph      = "★"
radius     = "14px"
blink      = "0.7s"
shine      = "inset 0 2px 0 0 rgba(255,255,255,0.55)"
menu_word  = "COPY"
empty_word = "NOTHING HERE!"
bar        = "bottom"
font_label = "Lilita One"
font_body  = "Baloo 2"

# What Dream Land drops (2026-08-23, user call): Kirby fights nothing here,
# carries no status ailments, and does not level. The HP line is the whole
# story. The rest of the creature furniture stays available to other skins.
[skins.features]
foe   = false
chips = false
exp   = false
lv    = false

# The speaker. A minimal creature block: no types, no nature, no level --
# just who is talking and what he carries. Types stay empty so the badge
# rows collapse on their data gate.
[skins.creature]
species      = "KIRBY"
ball         = "WARP STAR"
held_charge  = "MAXIM TOMATO"
held_battery = "PEP BREW"


# ---------------------------------------------------------------------------
# The three creature profiles from the creature-shell handoff (Aug 2026).
# A creature is a skin plus a [skins.creature] table: the palette is the
# creature's dark-field palette, and the creature block carries everything
# the shell surfaces print about the machine-as-creature (species, level,
# types, ability, held items, flavour note). The four category hues are
# skin-independent in this handoff (CMD/NET/TXT/SND are fixed), so all
# three creatures share them.
#
# The handoff shipped invented species (GRIMLING / PUDDLIN / FLOSSET) to
# stay clear of the trademarks; the user swapped in the real names for this
# personal machine (2026-08-19). The palettes are unchanged -- GENGAR here
# is still the handoff's shade-and-venom palette.
#
# Held items follow the power source (handoff turn 20a): LEFTOVERS on
# charge, GANLON BERRY on battery. The shell picks which one to show; both
# names live here so the choice is data, not code.

[[skins]]
slug    = "gengar"
name    = "GENGAR"
bg      = "#0a0713"
shadow  = "#04020a"
window  = "#241a3c"
outer   = "#e4d6ff"
inner   = "#4a2e6e"
cell    = "#1a1230"
strip   = "#130d22"
text    = "#f4edff"
body    = "#d2c2ee"
dim     = "#9a83c2"
accent  = "#c86bff"
outline = "#e4d6ff"
cmd     = "#7ef0c8"
net     = "#7cb8ff"
txt     = "#ffd36b"
snd     = "#ff86d0"

[skins.behavior]
blink      = "0.55s"
menu_word  = "FIGHT"
empty_word = "NO DATA"

[skins.creature]
species      = "GENGAR"
level        = 47
type1        = "GHOST"
type2       = "POISON"
type1_hue    = "#9c6bff"
type2_hue    = "#d45ab8"
sig          = "SHADOW BALL"
ability      = "LEVITATE"
nature       = "IMPISH"
ball         = "POKE BALL"
held_charge  = "LEFTOVERS"
held_battery = "GANLON BERRY"
# The real Red/Blue dex entry, at the user's request (2026-08-21).
note         = "Under a full moon, this POKEMON likes to mimic the shadows of people and laugh at their fright."

[[skins]]
slug    = "wooper"
name    = "WOOPER"
bg      = "#05101a"
shadow  = "#020810"
window  = "#123049"
outer   = "#cfe9ff"
inner   = "#1f5a80"
cell    = "#0c2233"
strip   = "#071824"
text    = "#eaf7ff"
body    = "#b6d8ee"
dim     = "#6f9cba"
accent  = "#ff9a3c"
outline = "#cfe9ff"
cmd     = "#7ef0c8"
net     = "#7cb8ff"
txt     = "#ffd36b"
snd     = "#ff86d0"

[skins.behavior]
blink      = "1s"
menu_word  = "FIGHT"
empty_word = "NO DATA"

[skins.creature]
species      = "WOOPER"
level        = 12
type1        = "WATER"
type2        = "GROUND"
type1_hue    = "#4ec8ff"
type2_hue    = "#c9a06a"
sig          = "MUD SHOT"
ability      = "WATER ABSORB"
nature       = "JOLLY"
ball         = "NET BALL"
held_charge  = "LEFTOVERS"
held_battery = "GANLON BERRY"
# The real Silver dex entry, at the user's request (2026-08-21).
note         = "A slippery film covers its body, making it hard to grasp. It burrows into soft mud to sleep."

[[skins]]
slug    = "whimsicott"
name    = "WHIMSICOTT"
bg      = "#0d1410"
shadow  = "#050a07"
window  = "#1e3226"
outer   = "#eaf7d8"
inner   = "#3d6b48"
cell    = "#142519"
strip   = "#0d1a12"
text    = "#f4fbe9"
body    = "#cbe2c2"
dim     = "#85a88f"
accent  = "#ff8fd0"
outline = "#eaf7d8"
cmd     = "#7ef0c8"
net     = "#7cb8ff"
txt     = "#ffd36b"
snd     = "#ff86d0"

[skins.behavior]
blink      = "1.4s"
menu_word  = "FIGHT"
empty_word = "NO DATA"

[skins.creature]
species      = "WHIMSICOTT"
type1        = "GRASS"
level        = 33
type2        = "FAIRY"
type1_hue    = "#8fe06a"
type2_hue    = "#ff9ad5"
sig          = "COTTON GUARD"
ability      = "PRANKSTER"
nature       = "CALM"
ball         = "LOVE BALL"
held_charge  = "LEFTOVERS"
held_battery = "GANLON BERRY"
# The real Black dex entry, at the user's request (2026-08-21).
note         = "Riding whirlwinds, they appear. These POKEMON sneak into homes and cause all sorts of mischief."
